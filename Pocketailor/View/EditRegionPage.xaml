<phone:PhoneApplicationPage
    x:Class="Pocketailor.View.EditRegionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">
	
	<phone:PhoneApplicationPage.ApplicationBar>
		<shell:ApplicationBar IsVisible="True">
			<shell:ApplicationBarIconButton x:Name="saveApplicationBarIconBtn" IconUri="/Images/Save.png" IsEnabled="True" Text="save" Click="saveApplicationBarIconBtn_Click" />
		</shell:ApplicationBar>
	</phone:PhoneApplicationPage.ApplicationBar>

	<phone:PhoneApplicationPage.Resources>
		<DataTemplate x:Key="RegionItemTemplate">
			<Grid>
				<CheckBox Content="{Binding Name}" IsChecked="{Binding Selected, Mode=TwoWay}" />
			</Grid>
		</DataTemplate>
	</phone:PhoneApplicationPage.Resources>

	<d:DataContext>
		<Binding Source="{StaticResource SampleDataSource}"/>
	</d:DataContext>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="{StaticResource TileBGColor}">
		<StackPanel>
			<TextBlock TextWrapping="Wrap" Text="Select regions" Margin="12,50,12,30" FontSize="48"/>
			<ListBox BorderThickness="12,0" SelectionMode="Multiple" ItemsSource="{Binding Regions}" ItemTemplate="{StaticResource RegionItemTemplate}" Height="551"/>
		</StackPanel>
    </Grid>

</phone:PhoneApplicationPage>